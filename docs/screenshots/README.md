# Screenshots for the portfolio README

The root README references three images that don't exist yet — add them here
before publishing. Capture them yourself rather than using ones generated in
a chat session, so you can control exactly what's visible.

| File | Screen | What to show / avoid |
|---|---|---|
| `overview.png` | Overview dashboard (`/`) | The stats cards (Discovered / Fully analyzed / Ready to pursue / In progress) and the "Next best action" panel. Fine to leave real numbers and your first name in the greeting. |
| `jobs.png` | Jobs workspace (`/#jobs`) | The filter bar (workflow stage, source, fit decision) and a few job cards. Company names and job titles are public information, so those are fine. Avoid opening a card that shows negative/candid fit-analysis text about a specific real company if you'd rather not have that public. |
| `profile.png` | Career profile → Skills tab (`/profile#skills`) | The Skills / Professional Strengths sections. **Crop or avoid the contact-info line** (phone number and email are in the page header) before saving. |

Optional extras if you want more coverage:

- Applications workspace showing the workflow-stage pipeline (Strategy →
  Draft → Review → Apply).
- The Chrome extension's capture button on a real job posting (blur the
  employer's page content if it's not yours to publish).

Once the three required files exist, the README's screenshot table and the
top banner image will render automatically — no other changes needed.
